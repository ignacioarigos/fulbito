# Football Tracker

App para controlar partidos de fútbol con amigos.

## Setup Local

1. Clona el repo y instala dependencias: `npm install`
2. Configura la DB: Crea una cuenta en [Neon](https://neon.tech) y obtén la `DATABASE_URL`.
3. Configura NextAuth: Obtén credenciales de Google OAuth en [Google Console](https://console.developers.google.com).
4. Crea `.env.local`:
